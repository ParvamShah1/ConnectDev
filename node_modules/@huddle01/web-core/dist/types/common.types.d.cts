import RemotePeer from '../RemotePeer.cjs';
import '../Consumer.cjs';
import 'mediasoup-client/lib/types';
import 'mediasoup-client/lib/RtpParameters';
import '../common-js/EnhancedEventEmitter.cjs';

type ConnectionState = 'uninitialized' | 'connecting' | 'connected' | 'awaiting-reconnection' | 'failed' | 'closed';
type NewPeerJoined = {
    peer: RemotePeer;
};
declare enum Role {
    HOST = "host",
    CO_HOST = "coHost",
    SPEAKER = "speaker",
    LISTENER = "listener",
    GUEST = "guest"
}

export { ConnectionState, NewPeerJoined, Role };
