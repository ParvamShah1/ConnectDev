import {
  useRemoteMedia
} from "./chunk-MV2DYDP7.js";

// src/hooks/useRemoteVideo.ts
var useRemoteVideo = (props) => {
  const { state, track, stream } = useRemoteMedia({
    label: "video",
    peerId: props.peerId,
    onStopped: props.onClose,
    onPlayable: (data) => {
      props.onPlayable?.({
        label: "video",
        track: data.track,
        stream: data.stream
      });
    }
  });
  const isVideoOn = stream ? true : false;
  return {
    state,
    track,
    stream,
    isVideoOn
  };
};

export {
  useRemoteVideo
};
//# sourceMappingURL=chunk-MKCRHSA2.js.map