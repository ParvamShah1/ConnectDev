import {
  useRemoteMedia
} from "./chunk-MV2DYDP7.js";

// src/hooks/useRemoteAudio.ts
var useRemoteAudio = (props) => {
  const { state, track, stream } = useRemoteMedia({
    label: "audio",
    peerId: props.peerId,
    onStopped: props.onClose,
    onPlayable: (data) => {
      props.onPlayable?.({
        label: "audio",
        track: data.track,
        stream: data.stream
      });
    }
  });
  const isAudioOn = stream ? true : false;
  return {
    state,
    track,
    stream,
    isAudioOn
  };
};

export {
  useRemoteAudio
};
//# sourceMappingURL=chunk-DU6KFTOY.js.map